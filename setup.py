from setuptools import setup

setup(name='msr605',
      version='1.0',
      description='An easy to is python interface to the MSR605 card reader',
      url='https://github.com/steeve/msr605',
      author='@Steeve, @Jmaxxz, @oxplot',
      packages=['msr605'],
      install_requires=[
      ],
      zip_safe=False)